{
  "name": "time-recorder",
  "version": "1.0.0",
  "description": "for tailwind build",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx tailwindcss build ./src/base.css -o ./src/main.css"
  },
  "author": "uta8a",
  "license": "MIT",
  "dependencies": {
    "autoprefixer": "^10.0.1",
    "postcss-cli": "^8.2.0",
    "tailwindcss": "^1.9.6"
  }
}
